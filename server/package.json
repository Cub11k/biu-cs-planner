{
  "name": "@biu-cs-planner/server",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "esbuild src/bin.ts --bundle --platform=node --format=esm --target=node22 --outfile=../dist/cli.js --banner:js='#!/usr/bin/env node' --legal-comments=eof --log-level=warning"
  },
  "dependencies": {
    "@biu-cs-planner/app": "*",
    "@biu-cs-planner/core": "*",
    "@hono/node-server": "^1",
    "hono": "^4"
  },
  "devDependencies": {
    "esbuild": "^0.25"
  }
}
