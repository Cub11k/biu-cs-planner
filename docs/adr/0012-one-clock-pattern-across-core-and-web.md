# One clock pattern across core and web

A clock string is `hh:mm`, zero-padded, `00:00` through `23:59` — one pattern, `/^([01]\d|2[0-3]):[0-5]\d$/`, written identically wherever a clock is validated, and one shared body inside every pattern that reads a clock at all. The schemas decide what a clock string is; no reader holds a looser opinion than the schema that admitted the data it reads.

Five files carry a clock pattern: `core/src/state/schema.ts`, `core/src/catalog/schema.ts`, `core/src/shoham/dialect.ts`, `web/src/timetable/week.ts`, and — since this decision — `core/src/clock.ts`. What none of them varies is not the whole pattern but its **body**, `([01]\d|2[0-3]):[0-5]\d` — the thing that says what an hour and a minute are. Every clock pattern in the repository is built from that body: the State File and Catalog schemas, `core/src/clock.ts`, the Shoham dialect's `isClockTime` and `web`'s week each anchor one copy of it, and `CLOCK_RANGE` in `core/src/shoham/dialect.ts` (issue #74) uses it twice with a separator between and anchors neither end, because it reads a range out of a Hebrew line rather than validating a field. Six patterns in five files, then, and seven copies of the body: `core/src/shoham/dialect.ts` carries two of the patterns. Counting files understates the copies, and comparing whole patterns would make a correct one look like drift. The body is written out rather than imported because `web` never imports `core` ([ADR-0002](0002-domain-behind-http-api-thin-ui.md)) and the schemas are the contract, not a derivation of it; `fixtures/clock-ranges.json` is the table both *readings* are tested against so that a change to either fails a test instead of drifting quietly (issue #48).

Keeping those spellings equal is a guarded invariant and not a judgement made in review: `tools/ci/clock-pattern.test.ts` (issue #89) walks the repository's source, finds every regex literal that reads a clock, and fails when one of them reads an hour and a minute any way other than through the body above. It names no file that carries a clock and pins no number of copies, so a sixth file that starts carrying a clock pattern is judged like the five that carry one today, and a pattern that wraps the body in something new — as `CLOCK_RANGE` does — passes without an exception being written for it. Where it cannot see, it says so: a clock pattern built by `new RegExp` from a string is not a regex literal and is outside its reach, and a colon with a digit matcher against it is as far as a text scan can judge, so it fails towards the loud side — a `host:port` parse reads as a clock and has to be answered rather than excused. `fixtures/clock-ranges.json` still pins the two *readings* against each other, `core/src/clock.ts` against `web/src/timetable/week.ts` (issue #48), which is the other question: not whether the pattern is the same, but whether the same pattern is read the same way.

`core/src/clock.ts` used to read `/^(\d{1,2}):([0-5]\d)$/`, a pattern inherited from the Clashes module, and so read `"9:00"` where every schema and `web` refused it. The reason given was that comparing clock strings directly is only correct while every one of them is zero-padded — `"9:00"` sorts after `"10:00"`, so a Clash would be hidden rather than reported — and a Blocked Time is student-entered. Narrowing the pattern dissolves that worry rather than answering it: within the padded set the strings sort chronologically anyway, so the trap needs no defence. Reading minutes stays, for the reason it was always really needed — an overlap is arithmetic on two ranges, and an `end` of `"00:00"` is 1440, a value no comparison of strings can produce.

What does not survive is *accepting* the unpadded spelling, which every schema admitting a span refuses first: `blockedTimeSchema` and `pickedMeetingSchema` in a State File, `meetingSchema` in a Catalog, `isClockTime` in the Shoham dialect. The ticket put it as a guard "against data the type system says cannot arrive"; strictly it is the schemas and not the types that refuse it — `WeeklySpan`'s `start` and `end` are plain `string`, a structural contract anything can satisfy, and `findMeetingClashes` is exported — so the guarantee is a validated path, not a compiled one. Either way the two halves of the app held opposite opinions about whether the defence was needed, which is what issue #54 exists to settle.

**Consequence, deliberately accepted: if an unpadded time can ever arrive, that is a schema bug to fix at the schema.** Not a reading to widen, and not a repair at the reader. A time the pattern refuses is read as no time at all, which keeps it out of every Clash rather than letting it collide with whatever encloses it — and nothing is blocked either way.

The consequence is accepted with its cost named, and the cost is the Clashes module's silence rather than a misnamed Warning on the State File path (issue #82, which reconciled this paragraph with the code). An unreadable clock is named where it is refused, and never downstream of that refusal: on the State File path `blockedTimeSchema` refuses the entry and `parseStateFile` reports `entry-dropped` naming `start` or `end`, and in the Shoham dialect the range is refused and reported as `meeting-unreadable` — a kind carrying no payload, and suppressed even so when `hours-do-not-divide` already explains the loss (issue #70). So `blocked-time-does-not-advance` in `core/src/state/file.ts` is not, as this paragraph used to say, the wrong Warning for "the clock could not be read". It is a Warning about a week rather than about a clock, and it never speaks for an unreadable one: what it reports, and is tested on, is `23:00`–`01:00` and `10:00`–`10:00`. What nothing can currently reach is the `undefined` half of its guard, held against this decision's own invariant breaking rather than against data. `core/src/state/file.test.ts` pins that unreachability from the side that matters: widen the *schema's* spelling without the clock's and the unreadable entry arrives at the range check, where that Warning would begin naming a cause that did not happen, and the test fails. Widening the clock alone changes nothing there, and is `tools/ci/clock-pattern.test.ts`'s to catch.

Inside the Clashes module an unreadable span produces no Warning at all — it simply Clashes with nothing. So a schema bug of this kind would surface as a Meeting silently taking part in no Clash, which is the price of having one pattern instead of a reader that second-guesses it. That cost was ruled acceptable on issue #71 and recorded beside the guards themselves by #78 — in `overlapOf` and in `meetingsOccupyingNoTime`, which withholds even a does-not-advance report for such a span because its `EmptyRangeShape` would have to guess which empty shape the span has (issue #52), where `blocked-time-does-not-advance` carries no shape. So the paths give one account and not two: a clock is named where it is refused, and a check downstream of a refusal speaks about the week or says nothing, never about the clock.

Two tests encoded the removed defence and were rewritten to assert the refusal instead: `core/src/timetable/clashes.test.ts` on a Blocked Time, and `core/src/shoham/overlaps.test.ts` on two Meetings of one Group. The second already said in its own comment that no current path produces an unpadded time; the first asserted the old reading without a reason, and now carries one.

## Considered Options

- **Widen `web` to match `core`:** both would accept `"9:00"`. It keeps a defence nothing needs and pays for it by making `web` read data *looser* than the API that validated it — the grid would place a time the State File it came from would have refused to store. It also leaves the pattern spelled two ways across five files, which is the drift issue #48 exists to stop.
- **Keep both readings and record the asymmetry:** put an unpadded range in `fixtures/clock-ranges.json` asserting that `core` is deliberately forgiving where `web` is strict, turning a drift into a recorded intent. Rejected because the intent is not one worth holding: a Blocked Time of `"9:00"`–`"11:00"` would Clash correctly in `core` and be drawn nowhere by `web`, and writing that down makes it permanent rather than fixed. The table now states no difference, and no unpadded row was added.
